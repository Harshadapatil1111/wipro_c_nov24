#include <stdio.h>

int main() {
    char c = 'a'; 

    if (islower(c)) {
        printf("%c is a small case letter.\n", c);
    } else {
        printf("%c is not a small case letter.\n", c);
    }

    return 0;
}
